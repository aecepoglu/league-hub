League-Hub
==========

## Development

The backend is written in Go exposes a GraphQL API.  
On the front-end we use ReactJS.

### Testing

Run all tests with `go test ./...`

You can find unit tests in the same folder as the code files.

The API tests are in the *test/* folder.
